const inquirer = require("inquirer");

/*
Functional Jest test from: https://jestjs.io/docs/getting-started
This exports sum which is then called @ sum.test.js by means of _ const sum = require('../index'); _

function sum(a, b) {
    return a + b;
  }
  module.exports = sum;
  */